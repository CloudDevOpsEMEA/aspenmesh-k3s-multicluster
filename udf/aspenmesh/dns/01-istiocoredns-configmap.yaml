apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: istiocoredns
    release: istiocoredns
  name: coredns
  namespace: istio-system
data:
  Corefile: |
    .:53 {
      errors
      health
      grpc aspendemo.org 127.0.0.1:8053
      forward . /etc/resolv.conf {
        except aspendemo.org
      }
      prometheus :9153
      cache 30
      loop
      reload
    }
